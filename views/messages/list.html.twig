<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Liste des messages</title>
</head>
<body>
    <h1>Liste des messages</h1>

    <a href="/CookinCrew/messages/create">Créer un nouveau message</a>

    <ul>
    {% for msg in messages %}
        <li>
            <h2>{{ msg.titre }}</h2>
            <p>{{ msg.description }}</p>
            {% if msg.image %}
                <img src="/CookinCrew/{{ msg.image }}" alt="Image message" width="200">
            {% endif %}
            <small>Posté le {{ msg.date_poste }}</small>
        </li>
    {% endfor %}
    </ul>
</body>
</html>
